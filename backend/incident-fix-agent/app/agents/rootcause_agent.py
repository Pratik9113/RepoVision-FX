"""
SUMMARY:
Analyzes shortlisted files and determines most likely root cause.
"""

def find_root_cause(files):
    # Dummy logic
    return {
        "file": files[0] if files else None,
        "reason": "Most keyword matches found here"
    }